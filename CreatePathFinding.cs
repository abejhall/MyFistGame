using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreatePathFinding : MonoBehaviour {

    WorldManager world;
	// Use this for initialization
	void Start () {
       // world = WorldManager.Instance;
       // Path_TileGraph tilegraph = new Path_TileGraph(WorldManager.Instance.world);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
